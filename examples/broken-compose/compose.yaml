```yaml
services:
  web:
    build: .
    container_name: doctor-docker-broken-web
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - APP_MODE=development
    volumes:
      - ./missing-folder:/app/data
    command: python app.py
